name: sample
commands:
  - name: fetch
    description: fetches some data
    rest:
      method: GET
      endpoint: https://reqres.in/api/users/{params.user-id}
      # no_status: true
      params:
        - name: user_id
          type: string
          required: true
